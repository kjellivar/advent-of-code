import assert from 'assert';
import { readLines } from '../lib/read-input.js';

// eslint-disable-next-line no-unused-vars
const input = readLines(2021, 1);

function part1() {}

function part2() {}

describe(`2021 - Day 1`, () => {
    it('part1 is ?', () => {
        assert.strictEqual(part1(), undefined);
    });

    it('part2 is ?', () => {
        assert.strictEqual(part2(), undefined);
    });
});
